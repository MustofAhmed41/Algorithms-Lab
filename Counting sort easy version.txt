#include <bits/stdc++.h>

using namespace std;
int arr2[] = {15,15,15,1,2,3,4,5,9,11,12, 0 ,0};

int main(){

    int n;
    int max=-1;
    for(int i = 0 ; i < sizeof(arr2)/sizeof(int);i++){
        if(arr2[i]>max)
            max = arr2[i];
    }
    vector<int> arr(max+1);

    for(int i =0;i<sizeof(arr2)/sizeof(int);i++){
        arr[arr2[i]]++;
    }
    for(int i= 0 ;i <= max; i++){
        while(arr[i]!=0){
            cout << i << " ";
            arr[i]--;
        }
    }

    return 0;
}
